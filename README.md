# baseline-css
A minimal, CSS-native utility framework. Zero build step. Fully CSP-compliant. Composable accessibility themes. Semantic design tokens. ~60% fewer tokens than Tailwind for LLM-friendly development.
